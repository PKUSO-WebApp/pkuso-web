# ============================================================
# PKUSO 交响乐团管理系统 — 环境变量
# 复制此文件为 .env.local 并填入实际值
# ============================================================

# ---- Supabase (必需) ----
# 在 Supabase Dashboard → Settings → API 页面获取
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# 服务端专用:绕过 RLS,仅用于 API route(如 notify)
# 在 Supabase Dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ---- Email / Resend (必需,排练通知用) ----
# 在 https://resend.com/api-keys 获取
RESEND_API_KEY=re_...
# 生产环境建议配置自定义发件人地址,详情见 Resend Domains
# RESEND_FROM_EMAIL=noreply@your-domain.com
# 也可用 NEXT_PUBLIC_RESEND_API_KEY(备用 key 名)

# ---- Supabase CLI & CI (可选,类型生成用) ----
# Project reference ID: Supabase Dashboard → Settings → General → Reference ID
SUPABASE_PROJECT_REF=abcdefghijklmnopqrst
# CI 中使用 supabase gen types 时需要
# 在 https://supabase.com/dashboard/account/tokens 创建
SUPABASE_ACCESS_TOKEN=...

# ---- 站点标识 (可选) ----
# 用于 OAuth 回调、邮件模板、SEO 等场景
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# NEXT_PUBLIC_APP_NAME=PKUSO 乐团管理

# ---- 错误监控 (可选,后续接入) ----
# SENTRY_AUTH_TOKEN=...
# NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...

# ---- 运维 (可选) ----
# NEXT_PUBLIC_MAINTENANCE_MODE=false
