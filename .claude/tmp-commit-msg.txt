fix: 请假已处理列表——撤回/取消不显示 + 审批后可见性修复

- 已处理列表仅 approved/rejected,withdrawn/canceled 不显示(STATUS 常量同步清理)
- 审批/驳回后不再本地删行,改为静默重拉,已处理 tab 立即可见
- fetch 加 keepOnError 模式:重拉失败保留旧列表、徽章不清零、详情弹窗不关闭
- fetch 加递增序号守卫,陈旧 GET 不再复活已处理行
- POST 响应 failed 明细并入 warnings 提示,重复审批/已处理不再静默无反馈
- reject 签名统一为 { ok, warnings },调用方与测试同步
- 详情弹窗操作按钮加载期间禁用(processing={busy || loading})
- 测试 +11(全量 966 全绿)

Closes #190
